# 문의 게시판 실서버 연결 방법 (Google Apps Script)

지금 상태로 `board.html`을 열면 "데모 모드"로 동작합니다 (이 브라우저에만 저장, 이메일 미발송).
아래 순서대로 하면 실제로 저장되고 이메일도 자동 발송됩니다. 전부 무료입니다.

## 1. 구글 시트 만들기
1. https://sheets.google.com 에서 새 스프레드시트 생성 (이름 예: `브라스레슨 문의목록`)
2. 상단 메뉴 `확장 프로그램` → `Apps Script` 클릭
3. 열린 편집기의 기본 코드를 모두 지우고, 이 프로젝트의 `apps-script/Code.gs` 내용을 전체 복사해서 붙여넣기
4. 코드 상단의 `OWNER_EMAIL` 값을 실제로 알림 받을 이메일 주소로 수정

## 2. 웹앱으로 배포
1. Apps Script 편집기에서 오른쪽 위 `배포` → `새 배포`
2. 유형 선택에서 `웹앱` 선택
3. 설정:
   - 실행 계정: **나(본인 계정)**
   - 액세스 권한이 있는 사용자: **모든 사용자**
4. `배포` 클릭 → 처음 실행 시 구글 계정 권한 승인 팝업이 뜨면 승인 (이메일 발송 권한 포함)
5. 배포 완료 후 나오는 **웹앱 URL**을 복사 (`https://script.google.com/macros/s/.../exec` 형태)

## 3. 사이트에 연결
1. `js/board.js` 파일을 연다
2. 맨 위 `const APPS_SCRIPT_URL = '';` 부분에 2번에서 복사한 URL을 붙여넣는다
   ```js
   const APPS_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycb.../exec';
   ```
3. 저장 후 `board.html`을 다시 열면 상단 "데모 모드" 배너가 사라지고, 실제로 시트에 저장 + 이메일 발송이 됩니다.

이후 코드를 수정하면(예: 이메일 문구 변경) Apps Script에서 `배포` → `배포 관리` → 연필 아이콘 → `새 버전`으로 다시 배포해야 반영됩니다.

## 4. (선택) 카카오톡 알림 추가하기
이메일만으로도 휴대폰에 즉시 알림이 오지만, 카카오톡 "나에게 보내기" 알림도 원하시면:

1. https://developers.kakao.com 에서 애플리케이션 생성
2. `카카오 로그인` 활성화, `talk_message` 동의항목(나에게 보내기) 추가 신청
3. 인가 코드 → 액세스 토큰 발급 (카카오 개발자 문서의 "카카오 로그인 REST API" 가이드 참고)
4. 발급받은 access token을 Apps Script 편집기의 `프로젝트 설정` → `스크립트 속성`에
   - 속성 이름: `KAKAO_ACCESS_TOKEN`
   - 값: 발급받은 토큰
   으로 저장
5. `Code.gs` 상단의 `ENABLE_KAKAO_NOTIFY`를 `true`로 변경 후 재배포

주의: 카카오 access token은 유효기간이 짧아(기본 12시간) 주기적으로 재발급이 필요합니다. 매번 수동 재발급이 번거로우면 이메일 알림만으로도 충분히 즉시성이 확보되므로, 이 단계는 생략해도 됩니다.

## 5. 문의 내용 확인하기
- 전체 문의(연락처 포함)는 1번에서 만든 구글 시트에서 확인
- `board.html`의 목록에는 개인정보 보호를 위해 연락처는 표시되지 않습니다

## 6. 후기 게시판 / 블로그 연동을 새로 추가했다면 (재배포 필수)

`apps-script/Code.gs`를 수정해서 후기(`review.html`)와 블로그(`blog.html`) 기능을 추가했습니다.
이 파일을 로컬에서 바꾼 것만으로는 아무 효과가 없고, **반드시 아래 순서로 재배포**해야
실제 사이트에 반영됩니다.

1. 1번에서 만든 구글 시트를 열고 `확장 프로그램 → Apps Script`로 이동
2. 편집기 내용을 전부 지우고 최신 `apps-script/Code.gs` 내용을 다시 붙여넣기
3. **반드시** `배포 → 배포 관리` → 기존 배포 항목의 연필(✎) 아이콘 클릭 → 버전: `새 버전` → `배포`
   - ⚠️ 여기서 `배포 → 새 배포`를 누르면 URL이 새로 발급되어 `js/board.js`, `js/reviews.js`,
     `js/blog.js`, `js/home-widgets.js`에 박아둔 기존 URL과 달라집니다. 그러면 문의 게시판은
     (예전 배포가 아직 살아있으니) 계속 되던 대로 동작하는데 후기/블로그만 원인 모르게 안 되는
     상황이 생기니, 꼭 "배포 관리 → 새 버전"으로 기존 URL을 유지하세요.
4. 재배포 후 브라우저 주소창에 웹앱 URL을 직접 열어서 `"version":"b64-v5"`가 보이면
   재배포가 정상 반영된 것입니다.
5. 첫 실행 시 권한 승인 팝업이 다시 뜰 수 있습니다 (URL 가져오기 권한 포함) → 승인

첫 방문 시 구글 시트에 `후기목록` 탭이 없으면 자동으로 생성됩니다(문의목록과 동일한 방식).

- 후기는 승인 절차 없이 작성 즉시 공개됩니다. 부적절한 후기를 지우려면 `후기목록` 시트에서
  해당 행을 직접 삭제하세요. 새 후기가 등록될 때마다 OWNER_EMAIL로 알림 메일이 오므로
  이것이 사실상 유일한 모니터링 수단입니다.
- 블로그 글은 네이버 블로그 RSS(`https://rss.blog.naver.com/raon92_.xml`)를 서버에서
  30분 캐시로 가져옵니다. 글을 새로 올려도 최대 30분 정도 지나야 사이트에 반영됩니다.

## 7. 체험레슨 신청(`trumpet-trombone-trial-lesson.html`)을 새로 추가했다면 (반드시 배포 후 공개)

`apps-script/Code.gs`에 체험레슨 신청(`type=trial`) 처리를 추가했습니다. 이 페이지를
사이트에 공개(링크 노출, 검색 노출 등)하기 **전에 반드시 먼저 재배포**하세요.

⚠️ 재배포 전에는 구버전 스크립트가 `type`을 무시하고 신청 내용을 `문의목록` 시트에
저장하는데, 이 시트는 `board.html` 공개 목록에 그대로 노출됩니다(연락처만 가려짐).
즉 재배포 전에 신청을 받으면 신청자의 이름과 희망 날짜/시간이 문의 게시판에 공개될
수 있습니다 — 체험레슨 신청을 개인 예약 정보로 취급해 공개 목록에서 뺀 이유와 정면으로
어긋나므로, "나중에 재배포"가 아니라 **공개 전 필수 선행 작업**입니다.

재배포 방법 (6번과 동일):
1. 구글 시트의 Apps Script 편집기에 최신 `Code.gs`를 붙여넣기
2. `배포 → 배포 관리` → 기존 배포의 연필(✎) 아이콘 → 버전: `새 버전` → `배포`
3. 재배포 후 웹앱 URL을 브라우저로 열어 `"version":"b64-v6"`이 보이면 정상 반영된 것입니다.

신청 내용은 구글 시트의 `체험레슨신청` 탭에서 확인합니다(문의목록과 동일하게 첫 신청 시
자동 생성). 문의 게시판과 달리 체험레슨 신청은 사이트에 공개 목록을 노출하지 않습니다
(이름·희망 일정이 담긴 개인 예약 정보이기 때문입니다).
