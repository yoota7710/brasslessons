# 문의 게시판 실서버 연결 방법 (Google Apps Script)

지금 상태로 `board.html`을 열면 "데모 모드"로 동작합니다 (이 브라우저에만 저장, 이메일 미발송).
아래 순서대로 하면 실제로 저장되고 이메일도 자동 발송됩니다. 전부 무료입니다.

## 1. 구글 시트 만들기
1. https://sheets.google.com 에서 새 스프레드시트 생성 (이름 예: `브라스레슨 문의목록`)
2. 상단 메뉴 `확장 프로그램` → `Apps Script` 클릭
3. 열린 편집기의 기본 코드를 모두 지우고, 이 프로젝트의 `apps-script/Code.gs` 내용을 전체 복사해서 붙여넣기
4. 코드 상단의 `OWNER_EMAIL` 값을 실제로 알림 받을 이메일 주소로 수정

## 2. 웹앱으로 배포
1. Apps Script 편집기에서 오른쪽 위 `배포` → `새 배포`
2. 유형 선택에서 `웹앱` 선택
3. 설정:
   - 실행 계정: **나(본인 계정)**
   - 액세스 권한이 있는 사용자: **모든 사용자**
4. `배포` 클릭 → 처음 실행 시 구글 계정 권한 승인 팝업이 뜨면 승인 (이메일 발송 권한 포함)
5. 배포 완료 후 나오는 **웹앱 URL**을 복사 (`https://script.google.com/macros/s/.../exec` 형태)

## 3. 사이트에 연결
1. `js/board.js` 파일을 연다
2. 맨 위 `const APPS_SCRIPT_URL = '';` 부분에 2번에서 복사한 URL을 붙여넣는다
   ```js
   const APPS_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycb.../exec';
   ```
3. 저장 후 `board.html`을 다시 열면 상단 "데모 모드" 배너가 사라지고, 실제로 시트에 저장 + 이메일 발송이 됩니다.

이후 코드를 수정하면(예: 이메일 문구 변경) Apps Script에서 `배포` → `배포 관리` → 연필 아이콘 → `새 버전`으로 다시 배포해야 반영됩니다.

## 4. (선택) 카카오톡 알림 추가하기
이메일만으로도 휴대폰에 즉시 알림이 오지만, 카카오톡 "나에게 보내기" 알림도 원하시면:

1. https://developers.kakao.com 에서 애플리케이션 생성
2. `카카오 로그인` 활성화, `talk_message` 동의항목(나에게 보내기) 추가 신청
3. 인가 코드 → 액세스 토큰 발급 (카카오 개발자 문서의 "카카오 로그인 REST API" 가이드 참고)
4. 발급받은 access token을 Apps Script 편집기의 `프로젝트 설정` → `스크립트 속성`에
   - 속성 이름: `KAKAO_ACCESS_TOKEN`
   - 값: 발급받은 토큰
   으로 저장
5. `Code.gs` 상단의 `ENABLE_KAKAO_NOTIFY`를 `true`로 변경 후 재배포

주의: 카카오 access token은 유효기간이 짧아(기본 12시간) 주기적으로 재발급이 필요합니다. 매번 수동 재발급이 번거로우면 이메일 알림만으로도 충분히 즉시성이 확보되므로, 이 단계는 생략해도 됩니다.

## 5. 문의 내용 확인하기
- 전체 문의(연락처 포함)는 1번에서 만든 구글 시트에서 확인
- `board.html`의 목록에는 개인정보 보호를 위해 연락처는 표시되지 않습니다
